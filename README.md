# Safe School - Система мониторинга температуры в школе

Safe School — это полнофункциональная система мониторинга температурных датчиков в школьных помещениях. Она позволяет:
- 📊 Визуализировать схемы этажей школы с датчиками температуры
- 🌡️ Отслеживать данные с датчиков в реальном времени
- 🔐 Безопасно передавать данные с использованием JWT токенов
- 📈 Хранить исторические данные для анализа

## 🏗️ Архитектура системы

Система состоит из трёх независимых компонентов:

### 1. **Сервер** (Flask) - `server/app.py`
- Принимает данные от датчиков температуры
- Проверяет подлинность запросов с помощью JWT
- Хранит последние 100 измерений для каждого датчика
- Имеет CORS поддержку для взаимодействия с веб-клиентом
- Запускается на `http://localhost:5000`

### 2. **Веб-клиент** (HTML/JavaScript) - `client/index.html`
- Графический интерфейс для создания и управления схемами этажей
- Позволяет размещать датчики на плане помещения
- Отображает данные с датчиков в реальном времени
- Полностью на русском языке
- Запускается в браузере на `http://localhost:8000`

### 3. **Симулятор датчиков** (Python/Tkinter) - `simulation/simulator.py`
- Имитирует работу 5 физических датчиков температуры
- Имеет интерфейс для изменения значений температуры
- Автоматически отправляет данные на сервер каждую секунду
- Полезен для тестирования без реальных датчиков

---

## 📋 Требования к системе

### Обязательные требования:
- **Python** версии 3.8 и выше (3.10+ рекомендуется)
- **Git** для клонирования репозитория
- Любой современный веб-браузер (Chrome, Firefox, Safari, Edge)
- **Интернет соединение** для установки пакетов (первый раз)

### Рекомендуемые требования:
- **4 термinal/окна** для одновременного запуска компонентов
- Операционная система: macOS, Linux или Windows (с WSL)
- Минимум 512 MB свободной памяти

### Как проверить готовность системы:

1. **Проверка Python:**
```bash
python3 --version
```
Должно вывести версию 3.8 или выше, например: `Python 3.10.12`

2. **Проверка Git:**
```bash
git --version
```
Должно вывести версию Git, например: `git version 2.42.0`

3. **Проверка npm/Node.js (опционально):**
```bash
node --version
npm --version
```
Это не требуется для базового функционала.

---

## 🚀 Установка и запуск

### Шаг 1: Клонирование репозитория

```bash
git clone <repository_url>
cd safe-school
```

### Шаг 2: Установка зависимостей сервера

Перейдите в папку сервера и установите необходимые пакеты:

```bash
cd server
pip install flask flask-cors pyjwt requests
```

### Шаг 3: Запуск сервера

В папке `server` запустите Flask приложение:

```bash
python3 app.py
```

Вы должны увидеть сообщение:
```
 * Running on http://127.0.0.1:5000
```

Сервер будет доступен по адресу: **http://localhost:5000**

> **Примечание:** Оставьте это окно терминала открытым — сервер должен работать в фоне

---

## 🌐 Открытие веб-приложения

### Способ 1: Прямой запуск файла

1. Откройте файл `client/index.html` в браузере:
   - На macOS/Linux: `open client/index.html`
   - На Windows: просто дважды кликните на файл в проводнике
   - Или перетащите файл в окно браузера

2. Вы должны увидеть интерфейс с кнопками управления и пустой холстом

### Способ 2: Запуск с помощью локального сервера (рекомендуется)

Для лучшей совместимости откройте клиент через локальный HTTP сервер:

```bash
cd client
# На macOS/Linux/WSL:
python3 -m http.server 8000

# Или на Windows:
python -m http.server 8000
```

Затем откройте браузер и перейдите на: **http://localhost:8000**

> **Примечание:** Оставьте это окно терминала открытым

---

## 📊 Запуск симулятора датчиков

В отдельном окне терминала перейдите в папку `simulation` и запустите симулятор:

```bash
cd simulation
pip install tkinter requests pyjwt
python3 simulator.py
```

Откроется окно с 5 виртуальными датчиками температуры (слайдеры). Вы сможете:
- **Менять значения температуры** — перетаскивайте слайдеры
- **Видеть отправку данных** — логи в консоли показывают каждую отправку

Данные будут автоматически отправляться на сервер каждую секунду.

---

## 🎮 Использование веб-приложения

### Основные функции:

1. **Рисовать** — Нажмите кнопку "Рисовать" для входа в режим рисования
   - Кликайте на холсте для добавления датчиков
   - Каждый датчик имеет уникальный ID

2. **Сохранить** — Сохраняет текущую схему этажа (активируется после рисования)

3. **Новый** — Создаёт новый пустой этаж

4. **Переключение этажей** — Используйте меню слева для переключения между этажами

### Отображение данных:
- Датчики показывают текущие значения температуры в реальном времени
- Данные обновляются по мере получения информации от симулятора

---

## 🔐 Безопасность

Система использует **JWT (JSON Web Tokens)** для аутентификации:
- Каждая школа получает уникальный токен
- Токен включен в заголовок `Authorization: Bearer <token>`
- По умолчанию используется тестовый школьный ID: `school_1`

**Важно:** В производстве замените `SECRET_KEY` на безопасный ключ!

---

## 📝 Структура проекта

```
safe-school/
├── server/
│   └── app.py              # Flask сервер с обработкой данных
├── client/
│   └── index.html          # Веб-интерфейс
├── simulation/
│   └── simulator.py        # Симулятор датчиков
└── README.md              # Этот файл
```

---

## 🛠️ Устранение проблем

### Ошибка: "Connection refused" при запуске симулятора
- Убедитесь, что сервер запущен на `http://localhost:5000`
- Проверьте, что порт 5000 не занят другим приложением

### Веб-интерфейс не загружается
- Попробуйте открыть клиент через локальный HTTP сервер (Способ 2)
- Проверьте консоль браузера на ошибки (F12 → Console)

### Данные не отправляются
- Убедитесь, что сервер работает (проверьте `http://localhost:5000/health`)
- Проверьте логи в консоли сервера

### Ошибка "ModuleNotFoundError"
- Убедитесь, что все зависимости установлены через pip
- Используйте `python3 -m pip install --upgrade pip`

---

## 📞 API Endpoints

### Health Check
```
GET /health
Response: { "status": "all okay" }
```

### Отправка данных датчика (требует JWT)
```
POST /sensor-data
Headers: Authorization: Bearer <token>
Body: {
  "sensor_id": "sensor_1",
  "value": 22.5,
  "timestamp": 1234567890
}
```

---

## 💡 Примеры использования

### Полный цикл тестирования:

```bash
# Терминал 1: Запуск сервера
cd server
python3 app.py

# Терминал 2: Запуск клиента (веб-сервер)
cd client
python3 -m http.server 8000

# Терминал 3: Запуск симулятора
cd simulation
python3 simulator.py

# Терминал 4: Браузер
# Откройте http://localhost:8000
```

Теперь вы можете:
1. Рисовать датчики на схеме в браузере
2. Менять значения температуры в окне симулятора
3. Видеть обновляющиеся данные на веб-интерфейсе

---

## 📄 Лицензия

Проект распространяется как есть. Используйте на свой риск.

---

## 🎯 Возможные расширения

- Добавить аутентификацию пользователей
- Внедрить базу данных (PostgreSQL, MongoDB)
- Добавить уведомления при превышении температуры
- Создать мобильное приложение
- Добавить экспорт отчётов (CSV, PDF)
- Реализовать аналитику и графики

---

**Версия:** 1.0  
**Последнее обновление:** Январь 2026
