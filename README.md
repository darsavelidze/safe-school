# 🏫 Safe School — интеллектуальная система мониторинга безопасности школы

Веб-приложение для мониторинга безопасности школьных помещений в реальном времени. Система объединяет данные с температурных датчиков и видеокамер, анализирует видеопоток нейросетью YOLOv8 для детекции людей и отображает всю информацию на интерактивной поэтажной схеме здания с автоматическими уведомлениями об аномалиях.

> **Проектная работа** для открытой городской научно-практической конференции «Инженеры будущего»  
> **Автор:** Проскурина Маргарита, ученица 10Б класса, ГБОУ Школа № 924  
> **Руководитель:** Дарсавелидзе Александр Александрович, учитель информатики  

---

## 📌 О проекте

В современных школах обеспечение безопасности — критически важная задача. Существующие системы (камеры видеонаблюдения, пожарные датчики) работают разрозненно и требуют постоянного ручного мониторинга. Ответственному за безопасность приходится контролировать обширную территорию с множеством слепых зон, а обнаружение инцидентов происходит постфактум.

Данный проект решает эту проблему: система в реальном времени собирает данные с датчиков температуры и видеокамер, обрабатывает видеопоток нейросетью YOLOv8 для подсчёта людей и визуализирует всё на интерактивной поэтажной карте школы — позволяя оперативно реагировать на угрозы.

### Ключевые особенности

- 🧠 **Детекция людей (YOLOv8)** — нейросеть анализирует видеопоток с камер и определяет количество людей в каждой зоне
- 🌡️ **Мониторинг температуры** — данные с датчиков в реальном времени с обнаружением аномалий (открытые окна, перегрев)
- 🗺️ **Интерактивная карта** — поэтажная схема школы с размещёнными датчиками и камерами, живое обновление данных
- 🔐 **JWT-аутентификация** — регистрация/авторизация школ, безопасная передача данных между компонентами
- 📡 **WebSocket** — мгновенная доставка обновлений с сервера на клиент без перезагрузки страницы
- 🎥 **Видеосимулятор** — GUI-приложение для имитации камер с загрузкой видеофайлов и потоковой отправкой кадров

---

## 🏗️ Структура проекта

```
├── server/
│   └── app.py                # Flask-сервер: REST API, JWT, WebSocket, YOLO-детекция, хранение данных
│
├── client/
│   └── index.html            # Веб-интерфейс: авторизация, интерактивная карта, визуализация данных
│
├── simulation/
│   ├── simulator.py          # Симулятор температурных датчиков (Tkinter GUI, 5 слайдеров)
│   └── video_simulator.py    # Симулятор видеокамер (загрузка видео, потоковая отправка кадров)
│
├── requirements.txt          # Зависимости Python (Flask, OpenCV, Ultralytics, PyJWT)
├── yolov8n.pt                # Веса модели YOLOv8 nano (скачиваются автоматически)
├── school_data.json          # Персистентное хранилище (схемы, позиции датчиков)
└── README.md
```

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/<ваш-username>/safe-school.git
cd safe-school
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate        # macOS / Linux
# venv\Scripts\activate         # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

Это установит все необходимые библиотеки: Flask, Flask-CORS, Flask-SocketIO, PyJWT, OpenCV, NumPy и Ultralytics (YOLOv8).

### 4. Запуск сервера

```bash
cd server
python app.py
```

Сервер будет доступен по адресу: http://localhost:5000

### 5. Запуск веб-клиента (в отдельном терминале)

```bash
cd client
python -m http.server 8000
```

Клиент будет доступен по адресу: http://localhost:8000

### 6. Запуск симулятора датчиков (в отдельном терминале)

```bash
cd simulation
python simulator.py
```

### 7. Запуск симулятора камер (опционально, в отдельном терминале)

```bash
cd simulation
python video_simulator.py
```

---

## 🎮 Как пользоваться

1. Откройте браузер и перейдите на http://localhost:8000
2. **Зарегистрируйте школу** — создайте аккаунт (ID школы + пароль)
3. **Создайте схему этажей** — нарисуйте поэтажный план, добавьте помещения
4. **Разместите датчики и камеры** — расставьте на карте точки мониторинга
5. **Запустите симулятор** — откройте simulator.py, введите ID школы, регулируйте температуру слайдерами
6. **Наблюдайте в реальном времени** — данные обновляются на карте через WebSocket
7. **Подключите видео** — через video_simulator.py загрузите видеофайл, YOLO подсчитает людей

### Компоненты системы

| Компонент | Описание |
|-----------|----------|
| Flask-сервер | REST API, JWT-аутентификация, YOLO-детекция, WebSocket, персистентное хранилище |
| Веб-клиент | SPA: авторизация, редактор поэтажных схем, размещение датчиков/камер, визуализация |
| Симулятор датчиков | Tkinter GUI с 5 слайдерами, отправка температуры на сервер каждую секунду |
| Симулятор камер | Tkinter GUI: загрузка видеофайлов, потоковая отправка кадров с настраиваемым FPS |

---

## 🔧 Как это работает

### Обработка видеопотока

```
Симулятор загружает видеофайл
       ↓
Извлечение кадров с заданным FPS
       ↓
Кодирование в Base64, отправка POST /video-frame + JWT
       ↓
Сервер декодирует кадр → OpenCV
       ↓
YOLOv8 nano: детекция объектов (класс "person", conf > 0.25)
       ↓
Подсчёт людей + рисование bounding boxes
       ↓
Результат → WebSocket → обновление карты в браузере
```

### Мониторинг температуры

```
Симулятор отправляет данные каждую 1 сек (POST /sensor-data + JWT)
       ↓
Сервер сохраняет последние 100 измерений для каждого датчика
       ↓
Клиент запрашивает данные → отображение на интерактивной карте
```

### Архитектура приложения

```
Браузер (JS + Canvas)                   Сервер (Flask + Python)
┌──────────────────────┐                ┌──────────────────────┐
│ Интерактивная карта   │ ── HTTP ──→   │  REST API            │
│ Авторизация           │ ← WebSocket ─ │  JWT-аутентификация   │
│ Визуализация данных   │               │  YOLO-детекция       │
└──────────────────────┘                │  Хранилище данных    │
                                        └──────────────────────┘
                                                  ↑
                              ┌────────────────────┴────────────────────┐
                              │                                         │
                  ┌───────────┴──────────┐              ┌──────────────┴──────────┐
                  │ Симулятор датчиков    │              │ Симулятор видеокамер     │
                  │ Tkinter, 5 датчиков  │              │ Tkinter, загрузка видео  │
                  │ POST /sensor-data     │              │ POST /video-frame        │
                  └──────────────────────┘              └─────────────────────────┘
```

- **server/app.py** — Flask-сервер: регистрация, авторизация (JWT), YOLO-детекция людей, WebSocket-обновления, хранение данных в JSON
- **client/index.html** — SPA: Canvas-редактор поэтажных схем, размещение датчиков/камер, живая визуализация данных
- **simulation/simulator.py** — Tkinter GUI с 5 слайдерами температуры, автоматическая отправка на сервер
- **simulation/video_simulator.py** — загрузка видеофайлов, потоковая отправка кадров на сервер для YOLO-анализа

---

## 📝 API Endpoints

### POST /sensor-data

Отправляет показания датчиков температуры.

**Request:** JSON, JWT в заголовке Authorization

**Response:**
```json
{ "success": true }
```

### POST /video-frame

Принимает кадр видеопотока и возвращает результат детекции.

**Request:** JSON (base64-кадр), JWT в заголовке Authorization

**Response:**
```json
{ "success": true, "people": 5 }
```

### GET /health

Проверка состояния приложения.

```json
{ "status": "ok" }
```

---

## 📋 Требования

- Python 3.8+
- Браузер с поддержкой ES6 (Chrome, Firefox, Safari, Edge)
- Flask 2.0+
- Flask-CORS
- Flask-SocketIO
- PyJWT
- OpenCV
- NumPy
- Ultralytics (YOLOv8)

---

## 🔮 Планы развития

- Тепловая карта температуры на поэтажной схеме
- Интеграция датчиков уровня шума
- Детекция бега по коридорам (анализ скорости перемещения)
- Обнаружение скоплений людей с уведомлениями
- Мобильное приложение для оперативного реагирования
- История событий и аналитика за день/неделю
- Docker-контейнеризация для упрощения развёртывания

---

## ⚠️ Важно

Система предназначена **для образовательных и демонстрационных целей**. Она не заменяет профессиональные системы безопасности и требует корректной настройки и эксплуатации.

---

## 📄 Лицензия

Проект создан в образовательных целях.
