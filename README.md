# Safe School - Система мониторинга температуры в школе

Safe School — это полнофункциональная система мониторинга температурных датчиков в школьных помещениях. Она позволяет:
- 📊 Визуализировать схемы этажей школы с датчиками температуры
- 🌡️ Отслеживать данные с датчиков в реальном времени
- 🔐 Безопасно передавать данные с использованием JWT токенов
- 📈 Хранить исторические данные для анализа

## 🏗️ Архитектура системы

Система состоит из трёх независимых компонентов, которые взаимодействуют между собой:

```
┌─────────────────────────────────────────────────────────────┐
│                    Веб-клиент (Браузер)                    │
│                  http://localhost:8000                      │
│         - Интерфейс для создания схем этажей              │
│         - Отображение данных с датчиков                    │
│         - Управление помещениями                           │
└─────────────────────────────────────────────────────────────┘
                            ↕️ (HTTP запросы)
┌─────────────────────────────────────────────────────────────┐
│                    Flask Сервер (Backend)                  │
│                  http://localhost:5000                      │
│         - REST API для получения данных                    │
│         - Хранилище данных датчиков (в памяти)            │
│         - JWT аутентификация                               │
└─────────────────────────────────────────────────────────────┘
                            ↕️ (POST /sensor-data)
┌─────────────────────────────────────────────────────────────┐
│                Симулятор датчиков (GUI)                     │
│               Tkinter окно с 5 слайдерами                  │
│         - Имитация работы физических датчиков             │
│         - Отправка данных на сервер каждую секунду        │
└─────────────────────────────────────────────────────────────┘
```

### 1. **Сервер** (Flask) - `server/app.py`
- **Назначение:** Обработка и хранение данных от датчиков
- **Функции:**
  - Принимает HTTP POST запросы с данными температуры
  - Проверяет JWT токены для безопасности
  - Хранит последние 100 измерений для каждого датчика
  - Имеет CORS поддержку для взаимодействия с веб-клиентом
  - Логирует все операции
- **Порт:** 5000
- **URL:** `http://localhost:5000`

### 2. **Веб-клиент** (HTML/JavaScript) - `client/index.html`
- **Назначение:** Графический интерфейс для управления системой
- **Функции:**
  - Создание и редактирование схем этажей
  - Размещение датчиков на плане помещения
  - Отображение реальных данных в реальном времени
  - Сохранение схем
  - Управление несколькими этажами
- **Язык интерфейса:** Русский
- **Порт:** 8000
- **URL:** `http://localhost:8000`

### 3. **Симулятор датчиков** (Python/Tkinter) - `simulation/simulator.py`
- **Назначение:** Имитация работы физических датчиков
- **Функции:**
  - Управление 5 виртуальными датчиками температуры
  - Изменение значений через слайдеры
  - Автоматическая отправка данных на сервер
  - Логирование процесса отправки
- **Интерфейс:** Tkinter (графическое окно)
- **Частота отправки:** Каждую 1 секунду

---

## 📋 Требования к системе

### ✅ Обязательные требования:

| Требование | Версия | Проверка |
|-----------|--------|----------|
| Python | 3.8+ (3.10+ рекомендуется) | `python3 --version` |
| Git | Любая новая версия | `git --version` |
| Браузер | Chrome, Firefox, Safari, Edge | Наличие браузера |
| ОС | macOS, Linux, Windows (WSL) | Автоматически |

### 🔧 Рекомендуемые требования:

- **Память:** Минимум 1 GB RAM (512 MB достаточно)
- **Место на диске:** 100 MB для проекта и зависимостей
- **Интернет:** Требуется для первичной установки пакетов
- **Терминалы:** Минимум 3 окна терминала для одновременной работы

### 🔍 Как проверить готовность системы:

#### 1. Проверка Python:
```bash
python3 --version
```

**Ожидаемый результат:**
```
Python 3.10.12
```

Если вы видите версию 3.8 или выше — всё в порядке ✅

Если Python не установлен, скачайте его с [python.org](https://www.python.org/downloads/)

#### 2. Проверка Git:
```bash
git --version
```

**Ожидаемый результат:**
```
git version 2.42.0
```

Если Git не установлен:
- **macOS:** Установите Xcode Command Line Tools: `xcode-select --install`
- **Linux:** `sudo apt-get install git` (Ubuntu) или `sudo yum install git` (Fedora)
- **Windows:** Скачайте с [git-scm.com](https://git-scm.com/download/win)

#### 3. Проверка окружения:
```bash
# Проверить, что Python может запустить скрипты
python3 -c "print('Python работает!')"

# Проверить наличие pip
pip3 --version
```

---

## 🚀 ПОЛНОЕ РУКОВОДСТВО ПО УСТАНОВКЕ И ЗАПУСКУ

### 📍 ЭТАП 1: Подготовка (Выполнить один раз)

#### Шаг 1.1: Клонирование репозитория

Откройте терминал и выполните:

```bash
# Если репозиторий на GitHub:
git clone https://github.com/username/safe-school.git
cd safe-school

# Или если у вас есть локальная папка:
cd /путь/к/safe-school
```

Проверьте, что вы в правильной папке:
```bash
ls -la
```

Вы должны увидеть:
```
drwxr-xr-x  server
drwxr-xr-x  client
drwxr-xr-x  simulation
-rw-r--r--  README.md
drwxr-xr-x  .git
```

#### Шаг 1.2: Установка зависимостей для сервера

Это нужно сделать только один раз.

```bash
cd server
pip3 install flask flask-cors pyjwt requests
```

**Подробное описание пакетов:**

| Пакет | Версия | Назначение |
|-------|--------|-----------|
| `flask` | 2.x+ | Веб-фреймворк для создания REST API |
| `flask-cors` | 4.x+ | Поддержка кросс-доменных CORS запросов |
| `pyjwt` | 2.x+ | Генерация и проверка JWT токенов |
| `requests` | 2.x+ | HTTP клиент для отправки запросов |

**Ожидаемый результат:**
```
Successfully installed flask-2.3.2 flask-cors-4.0.0 pyjwt-2.8.0 requests-2.31.0
```

**Проверка установки:**
```bash
python3 -c "import flask, flask_cors, jwt, requests; print('✅ Все пакеты установлены')"
```

#### Шаг 1.3: Установка зависимостей для симулятора

```bash
cd ../simulation
pip3 install requests pyjwt
```

**Для tkinter:**

На большинстве систем tkinter уже входит в Python. Проверьте:
```bash
python3 -c "import tkinter; print('✅ Tkinter доступен')"
```

Если ошибка на Linux:
```bash
# Ubuntu/Debian:
sudo apt-get install python3-tk

# Fedora:
sudo dnf install python3-tkinter

# Arch:
sudo pacman -S tk
```

На macOS и Windows tkinter поставляется с Python.

#### Шаг 1.4: Проверка структуры проекта

Убедитесь, что все файлы на месте:

```bash
cd ..  # Вернитесь в корень проекта
find . -type f -name "*.py" -o -name "*.html"
```

Вы должны увидеть:
```
./server/app.py
./client/index.html
./simulation/simulator.py
./README.md
```

---

### 🎯 ЭТАП 2: Запуск приложения (Выполнять каждый раз)

**ВАЖНО:** Для полноценной работы приложения необходимо одновременно запустить 3 компонента в разных терминальных сессиях. Это как запуск трёх программ одновременно.

#### ⚠️ Важное примечание о портах:

- **Сервер** будет слушать порт **5000**
- **Веб-сервер** будет слушать порт **8000**
- **Симулятор** не использует локальные порты (отправляет данные на 5000)

Если эти порты заняты другими приложениями, система не будет работать. Смотрите раздел "Устранение проблем" для решения конфликтов портов.

---

### 📍 ТЕРМИНАЛ 1: Запуск Flask сервера

Это самый важный компонент. Без него остальные не будут работать.

```bash
# Перейдите в папку сервера
cd /полный/путь/к/safe-school/server

# Запустите Flask приложение
python3 app.py
```

**Ожидаемый вывод при успешном запуске:**

```
 * Serving Flask app 'app'
 * Debug mode: off
 * WARNING in app.run(), this is a development server. Do not use it in production.
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

**✅ Сервер готов к работе!**

Проверить работу сервера можно в браузере:
```
http://localhost:5000/health
```

Вы должны увидеть:
```json
{"status":"all okay"}
```

**Что происходит в серверном логе:**

Каждый раз, когда симулятор или клиент отправляют данные, вы будете видеть логи:

```
2026-01-29 12:34:56,789 INFO Получены данные от школы school_1
2026-01-29 12:34:57,123 INFO Датчик sensor_1: 22.5°C
```

> **⚠️ НЕ закрывайте это окно терминала!** Сервер должен работать постоянно для остальных компонентов

---

### 📍 ТЕРМИНАЛ 2: Запуск веб-сервера (Клиент)

Откройте **НОВОЕ окно терминала** (не закрывайте первое!).

```bash
# Перейдите в папку клиента
cd /полный/путь/к/safe-school/client

# Запустите простой HTTP сервер Python
python3 -m http.server 8000
```

**Ожидаемый вывод при успешном запуске:**

```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

**✅ Клиент готов к использованию!**

Теперь откройте браузер и перейдите на:

```
http://localhost:8000
```

Вы должны увидеть:

1. **Заголовок:** "Схемы этажей"
2. **Три кнопки:** "Рисовать", "Сохранить", "Новый"
3. **Белый холст** в центре экрана
4. **Меню этажей** слева (изначально пусто)

**Если страница не загружается:**

1. Обновите страницу: `Ctrl+R` (Windows/Linux) или `Cmd+R` (macOS)
2. Проверьте консоль браузера на ошибки: `F12` → вкладка "Console"
3. Убедитесь, что оба терминала (сервер и веб-сервер) работают

> **⚠️ НЕ закрывайте это окно терминала!** Веб-сервер должен работать постоянно

---

### 📍 ТЕРМИНАЛ 3: Запуск симулятора датчиков

Откройте **ЕЩЁ ОДНО новое окно терминала**.

```bash
# Перейдите в папку симулятора
cd /полный/путь/к/safe-school/simulation

# Запустите симулятор
python3 simulator.py
```

**Ожидаемый вывод при успешном запуске:**

1. **В консоли:**
```
2026-01-29 12:34:56,123 INFO Отправка sensor_1: {'sensor_id': 'sensor_1', 'value': 20, 'timestamp': 1234567890}, ответ: 200 OK
2026-01-29 12:34:57,456 INFO Отправка sensor_2: {'sensor_id': 'sensor_2', 'value': 20, 'timestamp': 1234567891}, ответ: 200 OK
...
```

2. **Новое окно Tkinter:**
Откроется окно с заголовком "Температурные датчики (симуляция)"

**✅ Симулятор работает!**

---

### 🔍 Проверка всех компонентов

После запуска всех трёх компонентов, выполните полную проверку:

#### ✅ Проверка 1: Сервер работает

В браузере откройте:
```
http://localhost:5000/health
```

Вы должны увидеть:
```json
{"status":"all okay"}
```

#### ✅ Проверка 2: Веб-интерфейс загружается

В браузере откройте:
```
http://localhost:8000
```

Должны видеть интерфейс с кнопками и холстом.

#### ✅ Проверка 3: Симулятор отправляет данные

В **терминале 3** смотрите логи - должны видеть сообщения об отправке данных каждую секунду:
```
...ответ: 200 OK
```

#### ✅ Проверка 4: Сервер получает данные

В **терминале 1** должны видеть логи получения данных:
```
2026-01-29 12:34:56,789 INFO Датчик sensor_1: 20°C
```

**Если все 4 проверки пройдены успешно — система полностью функциональна!** 🎉

---

## 🎮 Подробное руководство по использованию веб-интерфейса

### 🖌️ Режим рисования (Создание схемы этажа)

1. **Нажмите кнопку "Рисовать"**
   - Кнопка должна стать активной (ярче)
   - Холст сменит цвет (станет готов к рисованию)

2. **Кликайте на холст для добавления датчиков**
   - Каждый клик добавляет новый датчик в эту точку
   - Датчику автоматически присваивается уникальный ID (sensor_1, sensor_2 и т.д.)
   - На холсте появляется визуальное представление датчика

3. **Сохранение схемы**
   - Когда вы закончили рисовать, нажмите кнопку "Сохранить"
   - Схема сохранится в памяти браузера
   - Её можно просмотреть позже

4. **Создание нового этажа**
   - Нажмите кнопку "Новый"
   - Холст очистится
   - Вы сможете создать схему другого этажа

### 📊 Отображение данных в реальном времени

После того, как вы разместили датчики и запустили симулятор:

1. **Датчики будут показывать текущую температуру**
   - Значение обновляется по мере получения данных от симулятора
   - Обновления происходят примерно каждую секунду

2. **Управление симулятором**
   - Откройте окно симулятора (Tkinter)
   - Перетягивайте слайдеры для изменения температуры
   - Измененные значения тут же будут отправлены на сервер
   - На веб-интерфейсе вы увидите обновленные значения

### 🏢 Работа с несколькими этажами

1. **Создание этажей:**
   - Нарисуйте схему первого этажа
   - Нажмите "Сохранить"
   - Нажмите "Новый" для второго этажа
   - Повторите для каждого этажа

2. **Переключение между этажами:**
   - Слева появится список сохраненных этажей
   - Кликайте на название этажа для переключения
   - Вы сможете видеть данные всех датчиков на выбранном этаже

---

## 🛠️ Подробное устранение проблем

### Проблема 1: "Connection refused" или "Failed to connect"

**Симптомы:**
- Симулятор выводит ошибку при попытке отправить данные
- В браузере ничего не загружается
- Сообщение типа: `Connection refused on port 5000`

**Причины и решения:**

```bash
# Проверка 1: Сервер запущен?
curl http://localhost:5000/health

# Если ошибка - сервер не запущен, запустите его:
cd server && python3 app.py

# Проверка 2: Порт 5000 свободен?
# На macOS/Linux:
lsof -i :5000

# На Windows:
netstat -ano | findstr :5000

# Если выводится процесс - он занял порт, завершите его или используйте другой порт
```

### Проблема 2: "ModuleNotFoundError: No module named 'flask'"

**Симптомы:**
```
Traceback (most recent call last):
  File "app.py", line 1, in <module>
    from flask import Flask
ModuleNotFoundError: No module named 'flask'
```

**Решение:**

Зависимости не установлены. Выполните:

```bash
# Убедитесь, что находитесь в папке server
cd server

# Установите все зависимости
pip3 install flask flask-cors pyjwt requests

# Проверьте установку
python3 -c "import flask; print(f'Flask {flask.__version__} OK')"
```

Если всё ещё ошибка:

```bash
# Обновите pip
pip3 install --upgrade pip

# Установите заново
pip3 install flask flask-cors pyjwt requests --force-reinstall
```

### Проблема 3: Веб-интерфейс не загружается

**Симптомы:**
- Браузер показывает "Cannot GET /"
- Страница не загружается вообще
- Серая ошибка страницы

**Решение:**

```bash
# Проверка 1: Веб-сервер работает?
curl http://localhost:8000

# Если ошибка - веб-сервер не запущен:
cd client
python3 -m http.server 8000

# Проверка 2: Откройте консоль браузера (F12)
# Смотрите вкладку "Console" на предмет ошибок

# Проверка 3: Обновите страницу
# Ctrl+R (Windows/Linux) или Cmd+R (macOS)

# Проверка 4: Очистите кеш браузера
# Ctrl+Shift+Delete или Cmd+Shift+Delete
```

### Проблема 4: Данные не отправляются на сервер

**Симптомы:**
- Симулятор не показывает сообщения об отправке
- На сервере нет логов получения данных
- Датчики на веб-интерфейсе не обновляются

**Решение:**

```bash
# Проверка 1: Оба сервера работают?
# В браузере откройте:
http://localhost:5000/health
http://localhost:8000

# Проверка 2: Смотрите логи симулятора
# В консоли симулятора должны быть сообщения об ошибках

# Проверка 3: Проверьте JWT токен
# Токен генерируется в simulator.py и должен быть правильным

# Проверка 4: Перезагрузите все компоненты
# 1. Завершите сервер (Ctrl+C)
# 2. Завершите веб-сервер (Ctrl+C)
# 3. Завершите симулятор (Ctrl+C)
# 4. Запустите все заново в правильном порядке
```

### Проблема 5: "Address already in use"

**Симптомы:**
```
Address already in use
OSError: [Errno 48] Address already in use
```

**Решение:**

Порт занят другим приложением. Есть несколько вариантов:

**Вариант 1: Завершить процесс, занявший порт**

```bash
# На macOS/Linux - найти процесс на порте 5000:
lsof -i :5000

# На Windows:
netstat -ano | findstr :5000

# Завершить процесс (замените PID на номер из вывода выше):
kill -9 <PID>  # macOS/Linux
taskkill /PID <PID> /F  # Windows
```

**Вариант 2: Использовать другой порт**

Если вы хотите использовать другой порт, отредактируйте:

- `server/app.py` - строка `app.run(port=5000)` → измените 5000 на, например, 5001
- `simulation/simulator.py` - строка `SERVER_URL = 'http://localhost:5000...'` → обновите 5000 на 5001

### Проблема 6: Tkinter окно не открывается

**Симптомы:**
```
ModuleNotFoundError: No module named 'tkinter'
```

**Решение:**

На Linux установите tkinter:

```bash
# Ubuntu/Debian:
sudo apt-get install python3-tk

# Fedora:
sudo dnf install python3-tkinter

# Arch:
sudo pacman -S tk
```

На macOS и Windows обычно уже установлено.

### Проблема 7: JWT ошибка "Invalid token"

**Симптомы:**
```
JWT error: Signature verification failed
Invalid token
```

**Решение:**

Проверьте, что SECRET_KEY одинаков в:
- `server/app.py`
- `simulation/simulator.py`

По умолчанию: `supersecretkey`

Если вы меняли этот ключ в одном месте, измените его везде.

---

## 📡 API Документация

Для разработчиков, которые хотят интегрировать систему с другими приложениями.

### Генерация JWT токена

```python
import jwt
import time

SECRET_KEY = 'supersecretkey'
SCHOOL_ID = 'school_1'

# Генерируем токен (действует 24 часа)
payload = {
    'school_id': SCHOOL_ID,
    'exp': int(time.time()) + 60*60*24
}
token = jwt.encode(payload, SECRET_KEY, algorithm='HS256')
print(f'Bearer {token}')
```

### Endpoint 1: Проверка здоровья сервера

**Метод:** `GET`  
**URL:** `http://localhost:5000/health`  
**Аутентификация:** Не требуется  

**Запрос:**
```bash
curl http://localhost:5000/health
```

**Ответ (200 OK):**
```json
{"status": "all okay"}
```

### Endpoint 2: Отправка данных датчика

**Метод:** `POST`  
**URL:** `http://localhost:5000/sensor-data`  
**Аутентификация:** Требуется JWT в заголовке `Authorization`

**Заголовки:**
```
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "sensor_id": "sensor_1",
  "value": 22.5,
  "timestamp": 1674986400
}
```

**Параметры:**
- `sensor_id` (string) - уникальный идентификатор датчика
- `value` (number) - значение температуры (любое число)
- `timestamp` (integer) - Unix timestamp (время в секундах)

**Пример с curl:**
```bash
curl -X POST http://localhost:5000/sensor-data \
  -H "Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9..." \
  -H "Content-Type: application/json" \
  -d '{
    "sensor_id": "sensor_1",
    "value": 22.5,
    "timestamp": 1674986400
  }'
```

**Успешный ответ (200 OK):**
```json
{"status": "data received", "school_id": "school_1"}
```

**Ошибка аутентификации (401):**
```json
{"error": "Invalid token"}
```

---

## 📝 Структура проекта

```
safe-school/
├── server/
│   └── app.py              # Flask сервер с обработкой данных
├── client/
│   └── index.html          # Веб-интерфейс
├── simulation/
│   └── simulator.py        # Симулятор датчиков
└── README.md              # Этот файл
```

---

## 🛠️ Устранение проблем

### Ошибка: "Connection refused" при запуске симулятора
- Убедитесь, что сервер запущен на `http://localhost:5000`
- Проверьте, что порт 5000 не занят другим приложением

### Веб-интерфейс не загружается
- Попробуйте открыть клиент через локальный HTTP сервер (Способ 2)
- Проверьте консоль браузера на ошибки (F12 → Console)

### Данные не отправляются
- Убедитесь, что сервер работает (проверьте `http://localhost:5000/health`)
- Проверьте логи в консоли сервера

### Ошибка "ModuleNotFoundError"
- Убедитесь, что все зависимости установлены через pip
- Используйте `python3 -m pip install --upgrade pip`

---

## 📞 API Endpoints

### Health Check
```
GET /health
Response: { "status": "all okay" }
```

### Отправка данных датчика (требует JWT)
```
POST /sensor-data
Headers: Authorization: Bearer <token>
Body: {
  "sensor_id": "sensor_1",
  "value": 22.5,
  "timestamp": 1234567890
}
```

---

## 🧪 Тестирование системы

### Полный цикл тестирования

1. **Запустите все компоненты (как описано выше)**

2. **Тест 1: Проверка сервера**
```bash
curl http://localhost:5000/health
# Должен вернуть: {"status":"all okay"}
```

3. **Тест 2: Проверка веб-интерфейса**
```bash
curl http://localhost:8000 | grep "Схема этажа"
# Должен найти заголовок в HTML
```

4. **Тест 3: Отправка тестовых данных**

```bash
# Генерируем токен
TOKEN=$(python3 -c "
import jwt, time
print(jwt.encode({'school_id': 'school_1', 'exp': int(time.time()) + 86400}, 'supersecretkey', algorithm='HS256'))
")

# Отправляем тестовые данные
curl -X POST http://localhost:5000/sensor-data \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"sensor_id": "test_sensor", "value": 25.0, "timestamp": '$(date +%s)'}'

# Должен вернуть: {"status":"data received","school_id":"school_1"}
```

5. **Тест 4: Проверка хранения данных**

Включите логирование в `server/app.py` для просмотра сохраненных данных:

```python
print(f"Все данные: {dict(data_store)}")
```

---

## 📊 Мониторинг и логирование

### Просмотр логов сервера

В терминале, где запущен сервер, вы видите все события:

```
2026-01-29 12:34:56,789 INFO Запуск Flask сервера
2026-01-29 12:34:57,123 INFO Получен запрос: POST /sensor-data
2026-01-29 12:34:57,456 INFO Датчик sensor_1: 22.5°C
```

### Просмотр логов симулятора

В терминале, где запущен симулятор:

```
2026-01-29 12:34:57,789 INFO Отправка sensor_1: {...}, ответ: 200 OK
2026-01-29 12:34:58,123 INFO Изменение sensor_1: 23.0°C
```

### Сохранение логов в файл

Для сохранения истории логов отредактируйте `server/app.py`:

```python
# Добавьте в начало файла:
handler = logging.FileHandler('server.log')
logging.getLogger().addHandler(handler)

# Теперь логи будут сохраняться в server.log
```

---

## 🔐 Безопасность и конфигурация

### Текущая конфигурация (для разработки)

- **SECRET_KEY:** `supersecretkey`
- **SCHOOL_ID:** `school_1`
- **JWT Expiration:** 24 часа
- **CORS:** Разрешены все источники

### ⚠️ Важно для продакшена

Перед развертыванием в продакшене:

1. **Измените SECRET_KEY на случайный длинный ключ:**

```python
# Генерируем безопасный ключ:
import secrets
print(secrets.token_urlsafe(32))
```

Вставьте результат в `server/app.py` и `simulation/simulator.py`

2. **Используйте переменные окружения:**

```python
import os
SECRET_KEY = os.getenv('SECRET_KEY', 'default_key')
```

3. **Включите HTTPS:**

Используйте сертификаты SSL/TLS для защиты данных в пути

4. **Используйте базу данных:**

Вместо хранения данных в памяти используйте PostgreSQL или MongoDB

5. **Настройте CORS правильно:**

Вместо разрешения всех источников, указывайте конкретные домены

---

## 💻 Примеры кода и использования

### Пример 1: Отправка данных с помощью Python

```python
import requests
import jwt
import time

# Константы
SERVER_URL = 'http://localhost:5000/sensor-data'
SECRET_KEY = 'supersecretkey'
SCHOOL_ID = 'school_1'

# Генерируем токен
payload = {'school_id': SCHOOL_ID, 'exp': int(time.time()) + 60*60*24}
token = jwt.encode(payload, SECRET_KEY, algorithm='HS256')
headers = {'Authorization': f'Bearer {token}'}

# Отправляем данные
data = {
    'sensor_id': 'sensor_1',
    'value': 23.5,
    'timestamp': int(time.time())
}

response = requests.post(SERVER_URL, json=data, headers=headers)
print(f"Status: {response.status_code}")
print(f"Response: {response.json()}")
```

### Пример 2: Отправка данных с помощью cURL

```bash
# Сначала генерируем токен (Python):
TOKEN=$(python3 -c "
import jwt, time
payload = {'school_id': 'school_1', 'exp': int(time.time()) + 86400}
print(jwt.encode(payload, 'supersecretkey', algorithm='HS256'))
")

# Отправляем данные
curl -X POST http://localhost:5000/sensor-data \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "sensor_id": "sensor_1",
    "value": 23.5,
    "timestamp": '$(date +%s)'
  }'
```

### Пример 3: Чтение данных с сервера (JavaScript)

```javascript
async function getSensorData(sensorId) {
  const token = 'Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...';
  
  const response = await fetch('http://localhost:5000/sensor-data', {
    method: 'GET',
    headers: {
      'Authorization': token,
      'Content-Type': 'application/json'
    }
  });
  
  const data = await response.json();
  console.log(data);
}
```

---

## 📝 Структура проекта (подробная)

```
safe-school/
│
├── server/
│   ├── app.py                    # Flask сервер (79 строк)
│   │   ├── Инициализация Flask
│   │   ├── CORS поддержка
│   │   ├── JWT аутентификация
│   │   ├── Хранилище данных (in-memory)
│   │   ├── Endpoint /health
│   │   └── Endpoint /sensor-data
│   │
│   └── requirements.txt           # (опционально) зависимости сервера
│
├── client/
│   ├── index.html                # Веб-интерфейс (417 строк)
│   │   ├── HTML структура
│   │   ├── CSS стили
│   │   ├── Canvas для рисования
│   │   ├── Управление этажами
│   │   └── JavaScript логика
│   │
│   └── style.css                 # (опционально) отдельный CSS файл
│
├── simulation/
│   ├── simulator.py              # Симулятор датчиков (67 строк)
│   │   ├── Tkinter интерфейс
│   │   ├── 5 слайдеров для датчиков
│   │   ├── Отправка на сервер
│   │   ├── JWT аутентификация
│   │   └── Логирование
│   │
│   └── requirements.txt           # (опционально) зависимости симулятора
│
├── README.md                      # Этот документ
│
└── .git/                          # Git репозиторий
```

---

## 🎯 Шаги для быстрого старта (Для нетерпеливых)

Если вы хотите быстро начать работу:

```bash
# 1. Клонируйте репозиторий
git clone <repo_url>
cd safe-school

# 2. Установите зависимости
pip3 install flask flask-cors pyjwt requests

# 3. В ПЕРВОМ терминале:
cd server && python3 app.py

# 4. Во ВТОРОМ терминале:
cd client && python3 -m http.server 8000

# 5. В ТРЕТЬЕМ терминале:
cd simulation && python3 simulator.py

# 6. В браузере откройте:
# http://localhost:8000
```

Всё! Система работает.

---

## 🚀 Возможные расширения и улучшения

Вот идеи для развития проекта:

### Функциональность:
- [ ] Аутентификация пользователей (логин/пароль)
- [ ] Множественные школы с отдельными учетными записями
- [ ] Уведомления при превышении температуры
- [ ] Экспорт отчетов (CSV, PDF)
- [ ] Графики и аналитика температурных данных
- [ ] Предупреждения в реальном времени

### Технология:
- [ ] Миграция на базу данных (PostgreSQL/MongoDB)
- [ ] Кеширование данных (Redis)
- [ ] Асинхронная обработка (Celery)
- [ ] Веб-сокеты для реального времени
- [ ] Контейнеризация (Docker)
- [ ] Развертывание на облаке (AWS, Google Cloud, Heroku)

### Интерфейс:
- [ ] Мобильное приложение (React Native, Flutter)
- [ ] Темный режим интерфейса
- [ ] Печать схем этажей
- [ ] Импорт чертежей зданий (PDF, CAD)

### Безопасность:
- [ ] Двухфакторная аутентификация
- [ ] Шифрование данных в пути (HTTPS)
- [ ] Ограничение по IP адресам
- [ ] Ревизионные логи (аудит)

---

## 📞 Поддержка и контакты

Если у вас возникли проблемы:

1. **Проверьте раздел "Устранение проблем"** выше
2. **Проверьте логи** в каждом из терминалов
3. **Проверьте консоль браузера** (F12)
4. **Перезагрузите все компоненты** в правильном порядке

---

## 📜 История версий

| Версия | Дата | Изменения |
|--------|------|-----------|
| 2.0 | 29.01.2026 | Полная переработка документации, детальные инструкции |
| 1.0 | 29.01.2026 | Первая версия, базовая функциональность |

---

## 📄 Лицензия

Проект распространяется как есть. Используйте на свой риск.

Для коммерческого использования свяжитесь с разработчиком.

---

## 🙏 Благодарности

Спасибо всем, кто использует эту систему и делает её лучше!

---

**Последнее обновление:** 29 января 2026  
**Версия документации:** 2.0 (полная и подробная)  
**Состояние:** Полностью рабочая ✅
